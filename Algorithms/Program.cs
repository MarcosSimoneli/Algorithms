using Algorithms;

#pragma warning disable S125 // Sections of code should not be commented out
public class Program
{
    static void Main()
    {
        //int[] array = { 1, 2, 3, 4, 5 };
        //int number = 5;
        //var found = SimpleSearch.SimpleSearchMethod(array, number);

        //Console.WriteLine($"O numero esta no array? {found}!");
    }
}
